---
phase: 05-about-services-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/founder-bio.tsx
  - src/components/sections/philosophy.tsx
  - src/components/sections/values-row.tsx
  - src/app/about/page.tsx
autonomous: true

must_haves:
  truths:
    - "About page displays a text-based founder bio for Nilda Bastone with professional tone"
    - "Philosophy section explains 'Why Ikigai?' covering alignment, integrity, human-centered leadership, and structural excellence"
    - "Values icon row renders all 5 values (Integrity, Accountability, Social Justice, Excellence, Courageous Leadership) with distinct icons"
    - "About page renders correctly on desktop, tablet, and mobile"
    - "About page uses alternating section backgrounds for visual rhythm"
  artifacts:
    - path: "src/components/sections/founder-bio.tsx"
      provides: "Founder introduction section with text-based bio (no photo)"
      exports: ["FounderBio"]
    - path: "src/components/sections/philosophy.tsx"
      provides: "Why Ikigai? philosophy section with four philosophy pillars"
      exports: ["Philosophy"]
    - path: "src/components/sections/values-row.tsx"
      provides: "Five-value icon row with Lucide icons and labels"
      exports: ["ValuesRow"]
    - path: "src/app/about/page.tsx"
      provides: "About page composing founder bio, philosophy, and values sections"
      exports: ["default", "metadata"]
  key_links:
    - from: "src/app/about/page.tsx"
      to: "src/components/sections/founder-bio.tsx"
      via: "direct import and render"
      pattern: "import.*FounderBio"
    - from: "src/app/about/page.tsx"
      to: "src/components/sections/philosophy.tsx"
      via: "direct import and render"
      pattern: "import.*Philosophy"
    - from: "src/app/about/page.tsx"
      to: "src/components/sections/values-row.tsx"
      via: "direct import and render"
      pattern: "import.*ValuesRow"
    - from: "src/components/sections/values-row.tsx"
      to: "src/lib/data/values.ts"
      via: "imports values content data"
      pattern: "import.*values.*from.*data/values"
---

<objective>
Build the About page with founder bio, philosophy, and values sections. This is the credibility and trust page that visitors check after the homepage.

Purpose: The About page establishes Nilda Bastone's authority and Ikigai's philosophy. It converts interest from the homepage into trust. Without this page, visitors who want to know "who is behind this?" will bounce.

Output: A complete About page at /about with three sections: founder bio (text-only, no photo), "Why Ikigai?" philosophy, and a values icon row.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build About page section components</name>
  <files>
    src/components/sections/founder-bio.tsx
    src/components/sections/philosophy.tsx
    src/components/sections/values-row.tsx
  </files>
  <action>
    Create three section components for the About page. All are Server Components (no 'use client'). All use the existing design system primitives (Section, Container, SectionHeading, Divider, Button) from `src/components/ui/`.

    **1. FounderBio (`src/components/sections/founder-bio.tsx`)**

    A text-based founder introduction section. NO photo (explicitly out of scope per REQUIREMENTS.md).

    Layout:
    - Section with `background="white"`
    - SectionHeading with tagline "Our Founder" and heading "Nilda Bastone"
    - Two-column layout on desktop (md:grid-cols-2), single column on mobile
    - Left column: Bio narrative text (2-3 paragraphs). Content should convey:
      - Nilda's expertise in organizational development and nonprofit leadership
      - Her experience building sustainable structures for purpose-driven organizations
      - Her passion for aligning mission with operational excellence
    - Right column: A styled blockquote or highlight box with a key quote about organizational architecture. Use a Card component or a styled div with a gold left border (border-l-4 border-gold) for the quote.
    - Import content from `@/lib/data/about.ts` (a content data file). If that file doesn't exist yet, hardcode the content directly in the component -- the executor should create the data file if the content data layer from Phase 3 is available, otherwise inline the content.

    Typography: Bio text in `text-body-lg` (Inter, 1.125rem). Quote in `font-serif text-h3` (Playfair Display). Name heading via SectionHeading.

    **2. Philosophy (`src/components/sections/philosophy.tsx`)**

    "Why Ikigai?" philosophy section explaining the firm's approach.

    Layout:
    - Section with `background="light-green"` (hunter-green-50)
    - SectionHeading with tagline "Our Philosophy" and heading "Why Ikigai?"
    - Brief intro paragraph explaining the name and philosophy
    - Four philosophy pillars in a 2x2 grid (md:grid-cols-2, gap-8):
      1. **Alignment** -- Organizations thrive when mission, strategy, and operations are in harmony
      2. **Integrity** -- Structures must be built on honest assessment and ethical foundations
      3. **Human-Centered Leadership** -- People are the living systems within every structure
      4. **Structural Excellence** -- Good intentions require strong architecture to create lasting impact

    Each pillar: A Card component with the pillar title as an h3 (font-serif, text-h4) and 2-3 sentences of description (font-sans, text-body). No icons needed -- the card structure provides visual rhythm.

    **3. ValuesRow (`src/components/sections/values-row.tsx`)**

    A horizontal row of 5 company values with icons.

    Layout:
    - Section with `background="hunter-green"` (dark background, white text)
    - SectionHeading with heading "Our Values", centered, dark mode (dark={true})
    - 5 values in a responsive row: flex-wrap on mobile (2-3 per row), all 5 in a row on desktop (lg:grid-cols-5)
    - Each value: centered layout with icon above, value name below

    Values and their Lucide React icons:
    1. **Integrity** -- `Shield` icon
    2. **Accountability** -- `Target` icon
    3. **Social Justice** -- `Scale` icon
    4. **Excellence** -- `Award` icon
    5. **Courageous Leadership** -- `Compass` icon

    Each value item:
    - Icon: `w-10 h-10 text-gold` (gold on dark background is accessible)
    - Value name: `text-body font-semibold text-white mt-3`
    - Centered within its grid cell: `flex flex-col items-center text-center`

    Import the values data from `@/lib/data/values.ts` if it exists (Phase 3 content layer). If not available, define the values array inline in the component. The executor should check for the data file and use it if present.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no type errors
    - All 3 component files exist in src/components/sections/
    - Each component exports a named function component
    - FounderBio uses two-column layout with text-only bio (no img tag)
    - Philosophy renders 4 philosophy pillars in a grid
    - ValuesRow renders 5 values with Lucide icons on dark background
    - No gold text on light backgrounds in any component
  </verify>
  <done>
    Three About page section components created: FounderBio (text-based bio with quote highlight), Philosophy (4-pillar grid on light-green background), ValuesRow (5 values with icons on hunter-green background). All use existing UI primitives and design tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose About page route</name>
  <files>
    src/app/about/page.tsx
  </files>
  <action>
    Create the About page route that composes the three section components.

    **src/app/about/page.tsx:**

    ```typescript
    import type { Metadata } from 'next'
    import { FounderBio } from '@/components/sections/founder-bio'
    import { Philosophy } from '@/components/sections/philosophy'
    import { ValuesRow } from '@/components/sections/values-row'

    export const metadata: Metadata = {
      title: 'About',
      description: 'Meet Nilda Bastone, founder of Ikigai Consulting Group. Learn about our philosophy of organizational architecture for purpose-driven organizations in Ontario.',
    }

    export default function AboutPage() {
      return (
        <>
          <FounderBio />
          <Philosophy />
          <ValuesRow />
        </>
      )
    }
    ```

    This is a thin composition layer -- no logic, just importing and arranging sections. The page is a Server Component (no 'use client').

    The section order creates visual rhythm:
    1. FounderBio (white background) -- establishes who
    2. Philosophy (light-green background) -- establishes why
    3. ValuesRow (hunter-green background) -- establishes principles

    The alternating backgrounds (white -> light-green -> hunter-green) create progressive deepening, both visually and conceptually.

    Optionally, add a CTA section at the bottom using the existing CTASection component if it exists from Phase 4. If not, skip it -- it can be added later.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds (static export produces about.html in out/)
    - `npm run dev` and navigating to /about shows all 3 sections
    - Section backgrounds alternate: white -> light-green -> hunter-green
    - Page has correct metadata (title: "About | Ikigai Consulting Group")
    - Page renders correctly on desktop and mobile (responsive check)
  </verify>
  <done>
    About page route created at /about composing FounderBio, Philosophy, and ValuesRow sections. Page has proper metadata for SEO. Sections alternate backgrounds (white, light-green, hunter-green) for visual rhythm. Page is a Server Component with static export support.
  </done>
</task>

</tasks>

<verification>
Phase 5 Plan 01 verification (About page -- PAGE-02):

1. About page is accessible at /about
   - `npm run dev` and navigate to localhost:3000/about

2. Founder bio section displays text-based bio for Nilda Bastone
   - Visual check: No photo/image, text-only bio
   - Two-column layout on desktop, stacked on mobile

3. Philosophy section explains "Why Ikigai?" with 4 pillars
   - Visual check: Light-green background, 2x2 grid of philosophy cards

4. Values icon row shows all 5 values with icons
   - Visual check: Hunter-green background, gold icons, white text
   - All 5 values visible: Integrity, Accountability, Social Justice, Excellence, Courageous Leadership

5. Responsive behavior
   - Desktop: Two-column bio, 2x2 philosophy grid, 5-value row
   - Mobile: Single column throughout

6. Design system compliance
   - Headings in Playfair Display (serif)
   - Body text in Inter (sans-serif)
   - Gold only used as decorative (icons, borders) -- never as text on light backgrounds
   - Generous section padding
</verification>

<success_criteria>
- About page renders at /about with all 3 sections
- Founder bio is text-only (no photo)
- Philosophy section has 4 pillars: Alignment, Integrity, Human-Centered Leadership, Structural Excellence
- Values row shows 5 values with Lucide icons on dark background
- Alternating section backgrounds (white -> light-green -> hunter-green)
- Page passes `npm run build` for static export
- Responsive layout works on desktop, tablet, and mobile
</success_criteria>

<output>
After completion, create `.planning/phases/05-about-services-pages/05-01-SUMMARY.md`
</output>
