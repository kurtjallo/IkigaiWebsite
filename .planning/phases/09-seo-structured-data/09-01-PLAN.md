---
phase: 09-seo-structured-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/metadata.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/about/page.tsx
  - src/app/services/page.tsx
  - src/app/model/page.tsx
  - src/app/impact/page.tsx
  - src/app/contact/page.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
autonomous: true

must_haves:
  truths:
    - "Each of the 6 pages has a unique title tag following the pattern '[Page Name] | Ikigai Consulting Group'"
    - "Each page has a unique meta description between 150-160 characters containing Ontario geographic and nonprofit sector keywords"
    - "Every page has self-referencing canonical URLs"
    - "Open Graph tags on every page render correct preview cards with title, description, image, and url"
    - "sitemap.xml is auto-generated at /sitemap.xml with all 6 page URLs plus privacy policy"
    - "robots.txt is auto-generated at /robots.txt allowing all crawlers and referencing the sitemap"
    - "Every page has a single H1 with logical H2/H3 hierarchy beneath it"
    - "All images and SVGs have descriptive alt text"
  artifacts:
    - path: "src/lib/data/metadata.ts"
      provides: "Complete per-page metadata definitions: titles, descriptions, OG tags, canonical URLs"
      exports: ["siteMetadata", "pageMetadata", "BASE_URL"]
    - path: "src/app/sitemap.ts"
      provides: "Auto-generated sitemap.xml with all page URLs, priorities, and change frequencies"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Auto-generated robots.txt with sitemap reference"
      exports: ["default"]
  key_links:
    - from: "src/lib/data/metadata.ts"
      to: "src/app/layout.tsx"
      via: "Root layout imports siteMetadata for default metadata"
      pattern: "import.*siteMetadata.*from.*metadata"
    - from: "src/lib/data/metadata.ts"
      to: "src/app/*/page.tsx"
      via: "Each page exports metadata using pageMetadata"
      pattern: "export const metadata.*=.*pageMetadata"
    - from: "src/app/sitemap.ts"
      to: "src/lib/data/metadata.ts"
      via: "Sitemap imports BASE_URL for generating page URLs"
      pattern: "import.*BASE_URL.*from.*metadata"
---

<objective>
Configure complete per-page SEO metadata, sitemap, robots.txt, and verify semantic HTML across all pages. This covers the "discoverability infrastructure" -- everything search engines need to properly index and display the site.

Purpose: Ontario's NGO ecosystem must be able to find Ikigai Consulting Group through search. Proper metadata with geographic and sector keywords, combined with sitemap/robots.txt and Open Graph tags, ensures the site ranks for "Ontario nonprofit consulting" queries and displays compelling preview cards on LinkedIn and social media.

Output: Updated metadata.ts with complete per-page SEO data, each page.tsx exporting correct metadata, auto-generated sitemap.xml and robots.txt, and verified heading hierarchy and alt text across all pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive per-page metadata and wire to all pages</name>
  <files>
    src/lib/data/metadata.ts
    src/app/layout.tsx
    src/app/page.tsx
    src/app/about/page.tsx
    src/app/services/page.tsx
    src/app/model/page.tsx
    src/app/impact/page.tsx
    src/app/contact/page.tsx
  </files>
  <action>
    Create the complete metadata system using Next.js Metadata API.

    **1. `src/lib/data/metadata.ts`** -- Create the centralized metadata file:

    Export a `BASE_URL` constant set to `'https://ikigaiconsulting.ca'` (assumed domain).

    Export `siteMetadata` as the root layout metadata with:
    - `metadataBase`: `new URL(BASE_URL)`
    - `title.default`: `'Ikigai Consulting Group | Organizational Architects for Purpose-Driven Organizations'`
    - `title.template`: `'%s | Ikigai Consulting Group'`
    - `description`: A 150-160 character description with Ontario + nonprofit keywords
    - `openGraph.type`: `'website'`
    - `openGraph.locale`: `'en_CA'`
    - `openGraph.url`: `BASE_URL`
    - `openGraph.siteName`: `'Ikigai Consulting Group'`
    - `openGraph.images`: `[{ url: '/images/og-image.png', width: 1200, height: 630, alt: 'Ikigai Consulting Group - Organizational Architects' }]`
    - `twitter.card`: `'summary_large_image'`
    - `robots`: `{ index: true, follow: true }`

    Export `pageMetadata` as a Record<string, Metadata> with entries for each page:

    **home** (used in app/page.tsx):
    - title: `'Organizational Architects for Purpose-Driven Organizations in Ontario'` (this overrides the template since it's the homepage)
    - description: `'Ikigai Consulting Group architects governance, strategy, and operations for Ontario nonprofits, NGOs, and social service agencies. Schedule a strategic conversation.'` (exactly 150-160 chars)
    - openGraph.title: `'Ikigai Consulting Group | Organizational Architects'`
    - openGraph.description: (same as description)
    - openGraph.url: `BASE_URL`
    - alternates.canonical: `BASE_URL`

    **about**:
    - title: `'About Us'` (template produces "About Us | Ikigai Consulting Group")
    - description: ~155 chars with keywords: Nilda Bastone, organizational architecture, Ontario nonprofits, purpose-driven leadership
    - openGraph with matching title/description
    - alternates.canonical: `${BASE_URL}/about`

    **services**:
    - title: `'Services'`
    - description: ~155 chars with keywords: 7 pillars, organizational architecture, strategic planning, governance, nonprofit consulting Ontario
    - openGraph with matching title/description
    - alternates.canonical: `${BASE_URL}/services`

    **model**:
    - title: `'The Ikigai Architecture Model'`
    - description: ~155 chars with keywords: proprietary framework, Blueprint Build Strengthen Sustain, organizational development Ontario
    - openGraph with matching title/description
    - alternates.canonical: `${BASE_URL}/model`

    **impact**:
    - title: `'Case Studies & Impact'`
    - description: ~155 chars with keywords: measurable results, nonprofit transformation, Ontario organizations, governance improvement
    - openGraph with matching title/description
    - alternates.canonical: `${BASE_URL}/impact`

    **contact**:
    - title: `'Contact'`
    - description: ~155 chars with keywords: schedule strategic conversation, organizational consulting Ontario, nonprofit consulting inquiry
    - openGraph with matching title/description
    - alternates.canonical: `${BASE_URL}/contact`

    IMPORTANT: Every description must be 150-160 characters. Count them. Include "Ontario" and a nonprofit-sector keyword in each one.

    **2. Update `src/app/layout.tsx`:**
    - Import `siteMetadata` from `@/lib/data/metadata`
    - Export `metadata` set to `siteMetadata`
    - This provides default title, description, and OG tags that pages override

    **3. Update each page.tsx:**
    Each page file must export metadata using the `pageMetadata` entries:

    ```typescript
    import type { Metadata } from 'next'
    import { pageMetadata } from '@/lib/data/metadata'

    export const metadata: Metadata = pageMetadata.about  // (or whichever page)
    ```

    The homepage (`src/app/page.tsx`) should use `pageMetadata.home`.

    Do NOT modify the component JSX of any page -- only add/update the metadata export.

    **4. Audit heading hierarchy:**
    Scan each page.tsx and its section components to verify:
    - Each page has exactly ONE `<h1>` (typically in the hero/header section)
    - `<h2>` elements are used for section headings
    - `<h3>` elements are used for sub-sections (pillar titles, card headings)
    - No heading levels are skipped (no h1 -> h3 without h2)

    If any heading hierarchy issues are found, fix them by updating the `headingAs` prop on SectionHeading components or adjusting raw heading elements. Document any changes made.

    **5. Audit alt text:**
    Scan all uses of `<img>`, `<Image>`, and `<svg>` elements across all components:
    - Every `<img>` and `<Image>` must have a descriptive `alt` attribute with relevant keywords
    - Decorative images should have `alt=""`
    - SVG components used as icons should have `aria-hidden="true"` if purely decorative, or `aria-label` if meaningful
    - The Ikigai Model diagram SVG must have a comprehensive `aria-label` describing the model

    Fix any missing or generic alt text found. Use descriptive, keyword-rich alt text (e.g., "Strategic Architecture pillar icon - organizational strategy consulting" not just "icon").
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no type errors
    - `npm run build` succeeds
    - Each page.tsx file has `export const metadata` that includes title, description, openGraph, and alternates.canonical
    - Every meta description is between 150-160 characters (verify by counting)
    - Every description contains "Ontario" or a geographic term
    - Every description contains a nonprofit/NGO sector keyword
    - grep confirms no page is missing `export const metadata`
    - View page source in dev mode: each page has unique `<title>` and `<meta name="description">`
    - View page source: OG tags are present (`og:title`, `og:description`, `og:image`, `og:url`)
    - Heading hierarchy audit: `grep -rn '<h[1-6]' src/` shows proper hierarchy
    - Alt text audit: `grep -rn '<img\|<Image' src/` shows alt on all images
  </verify>
  <done>
    Every page has unique title tags following "[Page Name] | Ikigai Consulting Group" pattern, unique 150-160 character meta descriptions with Ontario/nonprofit keywords, self-referencing canonical URLs, and Open Graph tags. Heading hierarchy is verified as single H1 per page with logical H2/H3 nesting. All images have descriptive alt text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create auto-generated sitemap.xml and robots.txt</name>
  <files>
    src/app/sitemap.ts
    src/app/robots.ts
  </files>
  <action>
    Create the sitemap and robots.txt generators using Next.js built-in features.

    **1. `src/app/sitemap.ts`:**

    ```typescript
    import type { MetadataRoute } from 'next'
    import { BASE_URL } from '@/lib/data/metadata'

    export default function sitemap(): MetadataRoute.Sitemap {
      return [
        {
          url: BASE_URL,
          lastModified: new Date(),
          changeFrequency: 'monthly',
          priority: 1.0,
        },
        {
          url: `${BASE_URL}/about`,
          lastModified: new Date(),
          changeFrequency: 'monthly',
          priority: 0.8,
        },
        {
          url: `${BASE_URL}/services`,
          lastModified: new Date(),
          changeFrequency: 'monthly',
          priority: 0.9,
        },
        {
          url: `${BASE_URL}/model`,
          lastModified: new Date(),
          changeFrequency: 'monthly',
          priority: 0.7,
        },
        {
          url: `${BASE_URL}/impact`,
          lastModified: new Date(),
          changeFrequency: 'monthly',
          priority: 0.7,
        },
        {
          url: `${BASE_URL}/contact`,
          lastModified: new Date(),
          changeFrequency: 'yearly',
          priority: 0.8,
        },
        {
          url: `${BASE_URL}/privacy`,
          lastModified: new Date(),
          changeFrequency: 'yearly',
          priority: 0.3,
        },
      ]
    }
    ```

    Priority rationale:
    - 1.0: Homepage (primary landing page)
    - 0.9: Services (key conversion page, pillar detail)
    - 0.8: About, Contact (trust + conversion endpoints)
    - 0.7: Model, Impact (thought leadership + social proof)
    - 0.3: Privacy (legal, low search value)

    **2. `src/app/robots.ts`:**

    ```typescript
    import type { MetadataRoute } from 'next'
    import { BASE_URL } from '@/lib/data/metadata'

    export default function robots(): MetadataRoute.Robots {
      return {
        rules: [
          {
            userAgent: '*',
            allow: '/',
          },
        ],
        sitemap: `${BASE_URL}/sitemap.xml`,
      }
    }
    ```

    This allows all crawlers to index all pages and points them to the sitemap. No pages need to be disallowed -- the site is entirely public content.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds
    - `ls out/sitemap.xml` exists in the static export output
    - `cat out/sitemap.xml` contains all 7 page URLs (home, about, services, model, impact, contact, privacy)
    - `ls out/robots.txt` exists in the static export output
    - `cat out/robots.txt` contains `Allow: /` and `Sitemap:` reference
    - All URLs in sitemap use the BASE_URL domain
  </verify>
  <done>
    sitemap.xml auto-generated with all 7 page URLs, correct priorities, and change frequencies. robots.txt auto-generated allowing all crawlers and referencing the sitemap. Both files appear in the static export output directory.
  </done>
</task>

</tasks>

<verification>
Requirements coverage check (SEO-01 through SEO-08):

1. SEO-01 (unique title tags): Each page exports metadata with unique title using template "[Page] | Ikigai Consulting Group"
2. SEO-02 (meta descriptions): Each page has 150-160 char description with Ontario + nonprofit keywords
3. SEO-03 (semantic HTML): Heading hierarchy audited -- single H1, logical H2/H3
4. SEO-04 (alt text): All images/SVGs audited for descriptive alt text
5. SEO-05 (sitemap + robots): sitemap.ts and robots.ts auto-generate correct files
6. SEO-06 (Open Graph): Every page has og:title, og:description, og:image, og:url
7. SEO-07 (canonical URLs): Every page has self-referencing canonical via alternates.canonical
8. SEO-08 (Ontario local SEO): Geographic terms in all descriptions, LocalBusiness schema (Plan 02)

Build verification:
- `npm run build` produces sitemap.xml and robots.txt in out/
- `npx tsc --noEmit` passes
- Page source inspection confirms unique titles, descriptions, OG tags, canonical URLs
</verification>

<success_criteria>
- Every page has unique title, description, OG tags, and canonical URL
- All meta descriptions are 150-160 characters with Ontario/nonprofit keywords
- sitemap.xml lists all pages with correct priorities
- robots.txt allows all crawlers and references sitemap
- Heading hierarchy: single H1 per page, logical H2/H3 nesting
- All images have descriptive alt text
- `npm run build` succeeds with all metadata files in output
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-structured-data/09-01-SUMMARY.md`
</output>
