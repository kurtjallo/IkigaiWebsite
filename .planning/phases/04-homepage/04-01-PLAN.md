---
phase: 04-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/hero.tsx
  - src/components/sections/problem.tsx
  - src/components/sections/solution.tsx
  - src/components/sections/pillars-grid.tsx
  - src/components/sections/who-we-serve.tsx
  - src/components/sections/why-ikigai.tsx
  - src/components/sections/cta-section.tsx
autonomous: true

must_haves:
  truths:
    - "Hero section renders full-width dark background with serif heading and gold CTA button"
    - "Problem section renders two-column layout with pain points list on the right"
    - "Solution section introduces the Ikigai Architecture Model with 7 pillar references"
    - "Pillars grid renders all 7 pillar cards in a responsive grid (1 col mobile, 2 col tablet, 3+4 or 4+3 desktop)"
    - "Who We Serve section lists all 5 target audiences"
    - "Why Ikigai section renders 3 columns (Authority, Full-Cycle Architecture, Measurable Impact)"
    - "CTA section renders dark background with gold button linking to contact"
  artifacts:
    - path: "src/components/sections/hero.tsx"
      provides: "Homepage hero with value proposition heading and gold CTA button"
      exports: ["HeroSection"]
    - path: "src/components/sections/problem.tsx"
      provides: "Two-column problem statement section"
      exports: ["ProblemSection"]
    - path: "src/components/sections/solution.tsx"
      provides: "Solution section introducing Ikigai Architecture Model"
      exports: ["SolutionSection"]
    - path: "src/components/sections/pillars-grid.tsx"
      provides: "7 Pillars card grid with icons, titles, descriptions, Learn More links"
      exports: ["PillarsGrid"]
    - path: "src/components/sections/who-we-serve.tsx"
      provides: "Target audience section with 5 audience segments"
      exports: ["WhoWeServe"]
    - path: "src/components/sections/why-ikigai.tsx"
      provides: "Three-column differentiators section"
      exports: ["WhyIkigai"]
    - path: "src/components/sections/cta-section.tsx"
      provides: "Reusable dark CTA banner with heading and gold button"
      exports: ["CTASection"]
  key_links:
    - from: "src/components/sections/pillars-grid.tsx"
      to: "src/lib/data/pillars.ts"
      via: "imports pillar data array"
      pattern: "import.*pillars.*from.*@/lib/data/pillars"
    - from: "src/components/sections/hero.tsx"
      to: "src/components/ui/button.tsx"
      via: "uses Button component for gold CTA"
      pattern: "import.*Button.*from.*@/components/ui/button"
    - from: "src/components/sections/pillars-grid.tsx"
      to: "src/components/ui/card.tsx"
      via: "uses Card component for pillar cards"
      pattern: "import.*Card.*from.*@/components/ui/card"
    - from: "src/components/sections/*.tsx"
      to: "src/components/ui/section.tsx"
      via: "all sections use Section wrapper for background and padding"
      pattern: "import.*Section.*from.*@/components/ui/section"
---

<objective>
Build all 7 homepage section components as standalone, self-contained Server Components.

Purpose: The homepage is the most important page -- it proves the entire design system works end-to-end by composing UI primitives (from Phase 1) with real content data (from Phase 3) inside the layout chrome (from Phase 2). Each section is an independent component that can be tested in isolation before being composed into the full page.

Output: 7 section components in `src/components/sections/` ready to be composed into the homepage in Plan 02.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md

Phase 1 deliverables (UI primitives): Button, Container, SectionHeading, Card, Divider, Section -- all in src/components/ui/
Phase 2 deliverables (layout chrome): Navigation, Footer, root layout -- in src/components/layout/ and src/app/layout.tsx
Phase 3 deliverables (content data): src/lib/data/pillars.ts (pillar definitions), src/components/svg/ (pillar icons, line motifs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero, Problem, and Solution section components</name>
  <files>
    src/components/sections/hero.tsx
    src/components/sections/problem.tsx
    src/components/sections/solution.tsx
  </files>
  <action>
    Create the first 3 homepage section components. All are Server Components (no 'use client' directive). Import and use UI primitives from `src/components/ui/` (Section, Container, SectionHeading, Button, Divider).

    **1. Hero Section (`src/components/sections/hero.tsx`)**

    - Wrap in `<Section background="hunter-green">` or use a custom full-height section (min-h-[85vh] or min-h-[80vh]) with hunter-green background
    - Large serif heading (text-h1 md:text-display, font-serif, text-white): "Architecting Purpose-Driven Organizations to Thrive"
    - Subtext (text-body-lg, text-neutral-300, max-w-[48rem], mt-6): "We design the structural foundations that transform mission-driven organizations from fragile to resilient."
    - Gold CTA Button (variant="gold", size="lg", href="/contact"): "Schedule a Strategic Conversation"
    - Center content vertically with flexbox (`flex items-center`)
    - Use Container for horizontal constraint
    - Add subtle architectural line motif as decorative background element if an SVG component exists in `src/components/svg/`, otherwise skip decoration for now (Phase 8 adds animation layer)

    **2. Problem Section (`src/components/sections/problem.tsx`)**

    - Wrap in `<Section background="white">`
    - SectionHeading: tagline "The Challenge", heading "Strong Missions. Fragile Structures.", align center
    - Two-column layout below heading: `grid md:grid-cols-2 gap-12 mt-12`
    - Left column: compelling narrative paragraph about organizations with strong missions but weak infrastructure. 2-3 paragraphs of body-lg text explaining how purpose-driven organizations struggle when their structures cannot support their ambitions.
    - Right column: list of 5 specific pain points, each as a short statement with a gold accent (small gold dash or bullet):
      1. "Strategic plans that gather dust on shelves"
      2. "Governance confusion that paralyzes decision-making"
      3. "Operational misalignment that drains resources"
      4. "Program drift that dilutes impact"
      5. "Leadership burnout from carrying the weight alone"
    - Style each pain point with `flex items-start gap-3` and a small gold accent element (e.g., a gold dash `<span className="w-4 h-0.5 bg-gold mt-3 shrink-0" />` or use a gold-colored dash character)

    **3. Solution Section (`src/components/sections/solution.tsx`)**

    - Wrap in `<Section background="light-green">`
    - SectionHeading: tagline "The Solution", heading "We Are Organizational Architects.", align center
    - Descriptive paragraph (text-body-lg, max-w-[48rem], mx-auto, text-center, mt-6): Explain that Ikigai Consulting Group designs and builds the structural foundations that organizations need -- not quick fixes, but architectural frameworks that support the full weight of their mission.
    - Below: introduce the Ikigai Architecture Model(TM) concept. Mention the 4-phase flow: Blueprint > Build > Strengthen > Sustain. This can be a simple horizontal flow visualization using 4 styled items in a row with arrows or dividers between them.
    - The flow visualization: `flex flex-col md:flex-row items-center justify-center gap-4 md:gap-8 mt-12`
    - Each phase: a Card or styled div with the phase name (font-serif, text-h4) and a one-line description
    - Use gold accents for arrows/connections between phases (e.g., `>` character styled in gold, or a small gold Divider between items)
    - Include TM symbol: "Ikigai Architecture Model&trade;" or use the Unicode TM character

    All content should use nonprofit sector language (DSGN-10). Avoid corporate jargon. Speak to leaders who carry mission.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no type errors
    - All 3 component files exist and export named components
    - Hero uses Section with hunter-green background and gold Button
    - Problem uses two-column grid layout
    - Solution introduces the 4-phase flow (Blueprint, Build, Strengthen, Sustain)
    - No 'use client' directive in any file
    - No gold text on light backgrounds
  </verify>
  <done>
    Hero, Problem, and Solution section components created as Server Components. Hero displays value proposition with gold CTA on hunter-green background. Problem uses two-column layout with pain points. Solution introduces the Ikigai Architecture Model with 4-phase flow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Pillars Grid, Who We Serve, Why Ikigai, and CTA section components</name>
  <files>
    src/components/sections/pillars-grid.tsx
    src/components/sections/who-we-serve.tsx
    src/components/sections/why-ikigai.tsx
    src/components/sections/cta-section.tsx
  </files>
  <action>
    Create the remaining 4 homepage section components. All are Server Components (no 'use client' directive). Import and use UI primitives from `src/components/ui/`.

    **4. Pillars Grid (`src/components/sections/pillars-grid.tsx`)**

    - Wrap in `<Section background="white">`
    - SectionHeading: tagline "Our Framework", heading "The 7 Pillars of Organizational Architecture", align center
    - Import pillar data from `src/lib/data/pillars.ts` -- the `pillars` array with slug, title, subtitle/description, icon
    - Grid layout: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8 mt-12`
    - Layout pattern: 3 columns on desktop creates a 3+3+1 layout with 7 items. Alternatively, use 4+3 by making the grid `lg:grid-cols-4` for the first row and centering the second row. The simplest approach is `lg:grid-cols-3` which gives a natural 3+3+1 with the last card spanning or left-aligned. Use `lg:grid-cols-3` for simplicity.
    - Each pillar card: use the Card component from `src/components/ui/card.tsx`
      - Inside each Card:
        - Pillar icon: Import the pillar's SVG icon component from `src/components/svg/` if it exists. If the SVG components are not yet available, use a placeholder icon from lucide-react (e.g., `Building2`, `Shield`, `Settings`, `BarChart3`, `Users`, `CheckCircle`, `Heart` -- one per pillar) as a temporary fallback. Style the icon: `w-10 h-10 text-gold mb-4`
        - Title (font-serif, text-h4, text-neutral-900): pillar.title
        - Description (text-body, text-neutral-600, mt-2): pillar.description or pillar.subtitle
        - "Learn More" link at the bottom: `<a href="/services#${pillar.slug}" className="text-hunter-green font-semibold text-sm mt-4 inline-block hover:text-gold transition-colors">Learn More &rarr;</a>`
    - The gold hover accent comes from the Card component's built-in hover behavior (DSGN-05)

    **5. Who We Serve (`src/components/sections/who-we-serve.tsx`)**

    - Wrap in `<Section background="hunter-green">`
    - SectionHeading: tagline "Who We Serve", heading "Built for Leaders Who Carry Mission.", align center, dark={true}
    - Grid or flex layout of 5 audience segments: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mt-12`
    - The 5 audiences (hardcode these -- they're small, stable content):
      1. "NGOs & Charities" -- "Organizations turning compassion into systemic change"
      2. "Social Service Agencies" -- "Teams delivering critical programs to communities in need"
      3. "Faith-Based Organizations" -- "Communities grounding their mission in purpose and service"
      4. "Women-Led & Justice-Centered Initiatives" -- "Leaders building equitable systems from the ground up"
      5. "Boards & Executive Teams" -- "Decision-makers seeking clarity, alignment, and impact"
    - Each audience item: a styled card-like div with `bg-hunter-green-600/30 border border-hunter-green-400/20 rounded-sm p-6`
      - Title (font-serif, text-h4, text-white): audience name
      - Description (text-body, text-neutral-300, mt-2): audience tagline
    - Dark background with white/light text throughout -- no gold text on this section

    **6. Why Ikigai (`src/components/sections/why-ikigai.tsx`)**

    - Wrap in `<Section background="light-green">`
    - SectionHeading: tagline "Why Ikigai", heading "What Sets Us Apart", align center
    - Three columns: `grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12 mt-12`
    - The 3 differentiators (hardcode content):
      1. **Authority**: "Deep Expertise, Not Generic Advice" -- "Over two decades of hands-on experience designing organizational structures for purpose-driven organizations. We do not consult from textbooks -- we architect from the trenches."
      2. **Full-Cycle Architecture**: "Blueprint to Sustain" -- "Most consultants deliver a report and leave. We design, build, strengthen, and sustain -- walking alongside you through every phase of transformation."
      3. **Measurable Impact**: "Results You Can See" -- "Every engagement produces tangible, measurable outcomes. From governance clarity to operational efficiency, our impact shows up in your numbers and your culture."
    - Each column: a styled div (NOT a Card -- these are on a light-green background, so Card's white bg would create a nested feel that works OR use Card if the visual works)
      - Small gold icon or accent at top (e.g., a gold line: `<div className="w-12 h-1 bg-gold mb-6" />`)
      - Title (font-serif, text-h3): differentiator name
      - Subtitle (font-semibold, text-hunter-green-600, text-sm, mt-2): subtitle
      - Description (text-body, text-neutral-600, mt-4): full description

    **7. CTA Section (`src/components/sections/cta-section.tsx`)**

    - Wrap in `<Section background="black">`
    - Center-aligned content: `text-center`
    - Heading (font-serif, text-h2, text-white): "Let's Architect Your Organization."
    - Subtext (text-body-lg, text-neutral-400, max-w-[36rem], mx-auto, mt-4): "Ready to build the structures your mission deserves? Start with a strategic conversation."
    - Gold CTA Button (variant="gold", size="lg", href="/contact"): "Book Your Strategy Call"
    - Optional: make this component accept props for heading/subtext/buttonText/buttonHref so it can be reused on other pages (About, Services, etc.). If you add props, keep the defaults as the values above.

    All content should use nonprofit sector language (DSGN-10). Strategic CTA language per DSGN-11 -- never generic "Contact Us" or "Submit".
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no type errors
    - All 4 component files exist and export named components
    - PillarsGrid imports from src/lib/data/pillars.ts and renders 7 cards
    - WhoWeServe lists 5 audience segments on dark background
    - WhyIkigai renders 3 columns with differentiator content
    - CTASection has gold button with strategic CTA text
    - No 'use client' directive in any file
    - No gold text on light backgrounds
    - "Learn More" links in pillars grid point to /services#slug
  </verify>
  <done>
    Pillars Grid, Who We Serve, Why Ikigai, and CTA section components created as Server Components. Pillars Grid renders all 7 pillars from data file with cards and Learn More links. Who We Serve displays 5 audiences on dark background. Why Ikigai shows 3-column differentiators. CTA section provides reusable dark banner with gold button.
  </done>
</task>

</tasks>

<verification>
After building all 7 section components:

1. `npx tsc --noEmit` -- zero TypeScript errors across all section files
2. All 7 files exist in src/components/sections/ with correct named exports
3. No 'use client' directive in any section component (all Server Components)
4. Each section uses Section wrapper from ui/ for background and padding
5. PillarsGrid imports and maps over pillar data from lib/data/pillars.ts
6. Hero and CTA use Button component with gold variant
7. Problem section uses two-column grid layout (DSGN-09)
8. Alternating backgrounds follow visual rhythm: hunter-green, white, light-green, white, hunter-green, light-green, black
9. No gold text on white or light backgrounds anywhere
10. All CTA text uses strategic language (DSGN-11), never "Contact Us" or "Submit"
</verification>

<success_criteria>
- 7 section components created and type-checking without errors
- All sections use UI primitives from Phase 1 (Section, Container, SectionHeading, Card, Button, Divider)
- PillarsGrid consumes pillar data from Phase 3's pillars.ts
- Two-column layout in Problem section
- Responsive grid in Pillars Grid (1/2/3 columns)
- Three-column layout in Why Ikigai
- Dark backgrounds on Hero, Who We Serve, and CTA sections
- Gold used only as decorative element (backgrounds, borders, accents) never as text on light backgrounds
- Strategic CTA language throughout
</success_criteria>

<output>
After completion, create `.planning/phases/04-homepage/04-01-SUMMARY.md`
</output>
