---
phase: 06-ikigai-model-case-studies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/model-diagram.tsx
  - src/components/sections/model-flow.tsx
  - src/app/model/page.tsx
autonomous: true

must_haves:
  truths:
    - "Ikigai Model page displays the SVG diagram large and centered with TM symbol on the model name"
    - "Model page explains the 4-phase flow: Blueprint > Build > Strengthen > Sustain"
    - "Model page shows how all 7 pillars integrate into the framework"
    - "SVG diagram has a comprehensive aria-label describing the model for screen readers"
    - "Page reads as thought leadership -- positions the framework as serious IP, not a throwaway graphic"
  artifacts:
    - path: "src/components/sections/model-diagram.tsx"
      provides: "Wrapper component for the Ikigai Architecture Model SVG with aria-label and TM branding"
      exports: ["ModelDiagram"]
    - path: "src/components/sections/model-flow.tsx"
      provides: "Blueprint > Build > Strengthen > Sustain flow explanation with pillar integration and why-it-works section"
      exports: ["ModelFlow"]
    - path: "src/app/model/page.tsx"
      provides: "Ikigai Model thought-leadership page composing diagram and flow sections"
      exports: ["default", "metadata"]
  key_links:
    - from: "src/components/sections/model-diagram.tsx"
      to: "src/components/svg/model-visual.tsx"
      via: "imports SVG component created in Phase 3"
      pattern: "import.*ModelVisual.*from.*svg/model-visual"
    - from: "src/components/sections/model-flow.tsx"
      to: "src/lib/data/pillars.ts"
      via: "imports pillar data for integration section"
      pattern: "import.*pillars.*from.*data/pillars"
    - from: "src/app/model/page.tsx"
      to: "src/components/sections/model-diagram.tsx"
      via: "composes diagram section into page"
      pattern: "import.*ModelDiagram"
---

<objective>
Build the Ikigai Model thought-leadership page -- the signature page that positions the Ikigai Architecture Model as serious proprietary IP.

Purpose: This page differentiates Ikigai from generic consultants. It presents the framework as a structured, intellectual methodology with a clear flow (Blueprint > Build > Strengthen > Sustain) and deep pillar integration. The page must convey "this is a real framework, not a marketing buzzword."

Output: A complete /model page with the SVG diagram displayed prominently, the 4-phase flow explained, pillar integration shown, and the TM symbol on every mention of the model name.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ModelDiagram section component</name>
  <files>
    src/components/sections/model-diagram.tsx
  </files>
  <action>
    Create the ModelDiagram section component that wraps the Ikigai Architecture Model SVG (created in Phase 3 at src/components/svg/model-visual.tsx) with proper accessibility and branding.

    This is a Server Component (no 'use client' directive).

    **Structure:**
    - Section wrapper with white background
    - SectionHeading with:
      - tagline: "Our Framework"
      - heading: "The Ikigai Architecture Model™" (use ™ character, not HTML entity -- this is JSX)
      - description: Brief intro positioning the model as a proprietary methodology for building resilient, purpose-driven organizations
      - align: center
    - Import and render the ModelVisual SVG component from `@/components/svg/model-visual`
    - Wrap the SVG in a container div with:
      - `role="img"` on the wrapping div
      - `aria-label` with a comprehensive description for screen readers: "The Ikigai Architecture Model diagram showing seven organizational pillars -- Strategic, Governance, Operational, Program, Leadership, Accountability, and Community Architecture -- arranged around a central purpose core, connected through four phases: Blueprint, Build, Strengthen, and Sustain."
      - Center the SVG with `mx-auto`
      - Max width constraint: `max-w-3xl` (48rem) so the diagram is large but not stretched on wide screens
      - Add padding: `mt-12 mb-8`

    **Important:**
    - The SVG component itself is created in Phase 3. This component WRAPS it with section layout, accessibility attributes, and branding. If the SVG component doesn't exist yet at execution time, create a placeholder div with the text "Ikigai Architecture Model Diagram" styled as a centered box with a dashed border, and add a code comment noting the SVG component will be provided by Phase 3.
    - The TM symbol (™) must appear on every mention of "Ikigai Architecture Model" on this page
    - Use Section and Container components from the design system (src/components/ui/)

    **Accessibility (A11Y-06):**
    - The wrapping div with role="img" and aria-label is the primary screen reader strategy
    - The SVG itself should have `aria-hidden="true"` since the wrapper provides the description (add this note as a comment for the Phase 3 SVG creator)
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - src/components/sections/model-diagram.tsx exists and exports ModelDiagram
    - Component includes role="img" and aria-label on SVG wrapper
    - TM symbol (™) appears in heading
    - aria-label text mentions all 7 pillars and 4 phases
  </verify>
  <done>
    ModelDiagram section component renders the Ikigai Architecture Model™ SVG (or placeholder) with comprehensive aria-label for screen readers, centered layout, and proper TM branding. A11Y-06 requirement is satisfied by the role="img" + aria-label pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build ModelFlow section and assemble the Model page</name>
  <files>
    src/components/sections/model-flow.tsx
    src/app/model/page.tsx
  </files>
  <action>
    **Part A: Create the ModelFlow section component (src/components/sections/model-flow.tsx)**

    This is a Server Component that explains the 4-phase flow and pillar integration.

    **Structure -- 3 sub-sections within one component:**

    **Sub-section 1: "The Four Phases" flow**
    - SectionHeading: tagline "How It Works", heading "From Blueprint to Sustain"
    - Display the 4 phases in a horizontal flow (desktop) / vertical stack (mobile):
      1. **Blueprint** -- "We assess your current structure, identify gaps, and design the architectural plan."
      2. **Build** -- "We implement foundational systems -- governance, strategy, operations -- with your team."
      3. **Strengthen** -- "We develop leadership capacity, program excellence, and accountability structures."
      4. **Sustain** -- "We embed community architecture and continuous improvement so the structure endures."
    - Each phase displayed as a numbered step (01, 02, 03, 04) with title (h3, serif) and description (body text)
    - Between phases on desktop: a gold arrow or gold divider line connecting them (use a simple → character or a small gold line, not a complex SVG)
    - Use a light-green background section for contrast

    **Sub-section 2: "Pillar Integration"**
    - SectionHeading: tagline "Complete Architecture", heading "Seven Pillars. One Integrated System."
    - Import pillars data from `@/lib/data/pillars.ts`
    - Display all 7 pillars in a grid (1 col mobile, 2 cols md, 3-4 cols lg)
    - Each pillar shown as a Card with:
      - Pillar title (h3, serif)
      - Brief description from pillar data
      - Link to services page anchor: href="/services#{pillar.slug}"
    - Use white background section
    - Explain that the pillars are not siloed -- they connect through the Blueprint > Build > Strengthen > Sustain flow

    **Sub-section 3: "Why It Works"**
    - SectionHeading on hunter-green background (dark: true): tagline "The Difference", heading "Why the Ikigai Architecture Model™ Works"
    - 3 reasons in a grid or list:
      1. **Holistic, Not Piecemeal** -- "Most consultants fix one thing. We architect the whole system."
      2. **Built to Last** -- "Our four-phase flow embeds sustainability from day one."
      3. **Purpose-Centered** -- "Every pillar connects back to your mission. Structure serves purpose, not the other way around."
    - Each reason: heading (h3) + description paragraph
    - CTA at bottom: Button with href="/contact", text "Schedule a Strategic Conversation", gold variant

    **Part B: Assemble the Model page (src/app/model/page.tsx)**

    Server Component page that composes the sections.

    ```typescript
    import type { Metadata } from 'next'
    import { ModelDiagram } from '@/components/sections/model-diagram'
    import { ModelFlow } from '@/components/sections/model-flow'

    export const metadata: Metadata = {
      title: 'The Ikigai Architecture Model',
      description: 'Our proprietary framework for building resilient, purpose-driven organizations. Seven pillars, four phases: Blueprint, Build, Strengthen, Sustain.',
    }

    export default function ModelPage() {
      return (
        <>
          <ModelDiagram />
          <ModelFlow />
        </>
      )
    }
    ```

    **Key notes:**
    - Every mention of the model name must include ™: "Ikigai Architecture Model™"
    - Use existing UI primitives: Section, Container, SectionHeading, Card, Button, Divider
    - Page is entirely Server Components -- no 'use client' needed
    - Content tone: authoritative, structured, intellectual. This is thought leadership, not a sales pitch.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds (static export generates out/model.html or out/model/index.html)
    - src/components/sections/model-flow.tsx exists and exports ModelFlow
    - src/app/model/page.tsx exists with metadata export
    - Page displays 4 phases (Blueprint, Build, Strengthen, Sustain) in order
    - Page displays all 7 pillars with links to /services#{slug}
    - "Why It Works" section has 3 reasons and a CTA button
    - ™ symbol appears on all mentions of "Ikigai Architecture Model"
  </verify>
  <done>
    Ikigai Model page is complete with three content areas: the SVG diagram with accessibility, the 4-phase flow explanation, and the 7-pillar integration with why-it-works section. Page exports Next.js metadata. All mentions of the model name include ™. CTA links to /contact. Page renders as a thought-leadership artifact positioning the framework as serious IP.
  </done>
</task>

</tasks>

<verification>
Phase 6 Plan 01 success criteria:

1. Model page accessible at /model route
   - `npm run build` produces static HTML for /model
   - Page renders with correct layout and section backgrounds

2. SVG diagram (or placeholder) displays prominently with TM branding
   - Diagram is centered, max-width constrained, visually dominant
   - "Ikigai Architecture Model™" heading with ™ character

3. Screen reader accessibility (A11Y-06)
   - SVG wrapper has role="img" and comprehensive aria-label
   - aria-label mentions all 7 pillars and 4 phases by name

4. Flow explanation is clear and structured
   - 4 phases displayed in order: Blueprint > Build > Strengthen > Sustain
   - Each phase has numbered step, title, and description

5. Pillar integration connects to services page
   - All 7 pillars displayed in grid with links to /services#{slug}

6. Page tone conveys thought leadership
   - Authoritative content, not sales-y
   - CTA uses strategic language ("Schedule a Strategic Conversation")
</verification>

<success_criteria>
- /model page renders with diagram, flow, pillars, and CTA
- ™ symbol on all mentions of "Ikigai Architecture Model"
- SVG wrapper has role="img" + comprehensive aria-label (A11Y-06)
- 4 phases explained: Blueprint > Build > Strengthen > Sustain
- 7 pillars displayed with links to /services#{slug}
- "Why It Works" section with 3 differentiation points
- CTA button links to /contact with strategic language
- `npm run build` succeeds
- `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-ikigai-model-case-studies/06-01-SUMMARY.md`
</output>
